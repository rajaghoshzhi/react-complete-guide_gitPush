const initialState = {
    ingredients:'',
    totalPrice: 100
}
const rootReducer = (state = initialState,action) => {

    switch(action.type){
        case 'ADD_INGREDIENTS':
            
    }

    return state;

}

export default rootReducer;
